[project]
name = "twotakt"
version = "0.2.0"
description = "Discrete-event simulation methodology and examples (SimPy)."
requires-python = ">=3.11"
# v2: examples are self-contained; per-example requirements.txt declare deps.
# This file is kept as a workspace marker.
dependencies = []
